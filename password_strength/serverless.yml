service: password-strength

provider:
  name: aws
  runtime: nodejs12.x

plugins:
  - serverless-offline

functions:
  password:
    handler: handler.password
    events: 
      - http:
          path: password/{password}
          method: get
